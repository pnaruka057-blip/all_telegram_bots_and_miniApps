<!-- FIXED TOP BAR (status bar + custom bar) -->
<div class="fixed left-0 right-0 top-0 z-[9999] border-b border-slate-200 backdrop-blur
         dark:border-slate-800">
  <!-- height = safe-top + 52px -->
  <div class="mx-auto max-w-2xl px-4 flex items-center" style="
      padding-top: var(--app-safe-top, 0px);
      height: calc(var(--app-safe-top, 0px) + 52px);
    ">
  </div>
</div>